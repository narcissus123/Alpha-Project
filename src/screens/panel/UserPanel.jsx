import { UserPanelContainer } from "../../components/userPanelContainer/UserPanelContainer";

const UserPanel = (props) => {
  return <UserPanelContainer programInfo={props.programInfo} />;
};

export { UserPanel };
